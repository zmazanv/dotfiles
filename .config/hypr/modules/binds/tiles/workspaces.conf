###############################################################################
# $HOME/.config/hypr/modules/binds/workspaces.conf
#
# [HYPRLAND] - CONFIGURACIÓN DE MAPAS DE TECLAS DE ESPACIOS DE TRABAJO
#
# Véase [https://wiki.hyprland.org/Configuring/Dispatchers/]
###############################################################################
# $HOME/.config/hypr/modules/binds/workspaces.conf
#
# [HYPRLAND] - WORKSPACES KEYMAPS CONFIGURATION
#
# See [https://wiki.hyprland.org/Configuring/Dispatchers/]
###############################################################################
general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 20
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 10
    blur = yes
    blur_size = 3
    blur_passes = 1
    blur_new_optimizations = on

    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = on
    workspace_swipe_fingers = 3
    workspace_swipe_create_new = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic-mouse-v1 {
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
#
misc {
    vrr = 1
}

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, M, killactive, 
bind = $mainMod + CONTROL, Q, exit, 
bind = $mainMod + CONTROL + SHIFT, N, togglefloating, 
bind = $mainMod + CONTROL, N, fullscreen, 0 # dwindle
bind = $mainMod + SHIFT, N, fullscreen, 1 # dwindle



# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bind = $mainMod + CONTROL, H, resizeactive, -100 0
bind = $mainMod + CONTROL, J, resizeactive, 0 100
bind = $mainMod + CONTROL, K, resizeactive, 0 -100
bind = $mainMod + CONTROL, L, resizeactive, 100 0

bind = $mainMod, N, togglesplit, # dwindle

#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el foco a la izquierda usando:
# 1. { $mainMod (+) H }
#------------------------------------------------------------------------------
# Move focus left using:
# 1. { $mainMod (+) H }
#------------------------------------------------------------------------------
bind = $mainMod, H, movefocus, l
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el foco hacia abajo usando:
# 1. { $mainMod (+) J }
#------------------------------------------------------------------------------
# Move focus down using:
# 1. { $mainMod (+) J }
#------------------------------------------------------------------------------
bind = $mainMod, J, movefocus, d
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el foco hacia arriba usando:
# 1. { $mainMod (+) K }
#------------------------------------------------------------------------------
# Move focus up using:
# 1. { $mainMod (+) K }
#------------------------------------------------------------------------------
bind = $mainMod, K, movefocus, u
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el foco a la derecha usando:
# 1. { $mainMod (+) L }
#------------------------------------------------------------------------------
# Move focus right using:
# 1. { $mainMod (+) L }
#------------------------------------------------------------------------------
bind = $mainMod, L, movefocus, r
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------

#------------------------------------------------------------------------------
# Cambia al espacio de trabajo 1 usando:
# 1. { $mainMod (+) 1 }
#------------------------------------------------------------------------------
# Switch to workspace 1 using:
# 1. { $mainMod (+) 1 }
#------------------------------------------------------------------------------
bind = $mainMod, 1, workspace, 1
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Cambia al espacio de trabajo 2 usando:
# 1. { $mainMod (+) 2 }
#------------------------------------------------------------------------------
# Switch to workspace 2 using:
# 1. { $mainMod (+) 2 }
#------------------------------------------------------------------------------
bind = $mainMod, 2, workspace, 2
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Cambia al espacio de trabajo 3 usando:
# 1. { $mainMod (+) 3 }
#------------------------------------------------------------------------------
# Switch to workspace 3 using:
# 1. { $mainMod (+) 3 }
#------------------------------------------------------------------------------
bind = $mainMod, 3, workspace, 3
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Cambia al espacio de trabajo 4 usando:
# 1. { $mainMod (+) 4 }
#------------------------------------------------------------------------------
# Switch to workspace 4 using:
# 1. { $mainMod (+) 4 }
#------------------------------------------------------------------------------
bind = $mainMod, 4, workspace, 4
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Cambia al espacio de trabajo 5 usando:
# 1. { $mainMod (+) 5 }
#------------------------------------------------------------------------------
# Switch to workspace 5 using:
# 1. { $mainMod (+) 5 }
#------------------------------------------------------------------------------
bind = $mainMod, 5, workspace, 5
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Cambia al espacio de trabajo 6 usando:
# 1. { $mainMod (+) 6 }
#------------------------------------------------------------------------------
# Switch to workspace 6 using:
# 1. { $mainMod (+) 6 }
#------------------------------------------------------------------------------
bind = $mainMod, 6, workspace, 6
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Cambia al espacio de trabajo 7 usando:
# 1. { $mainMod (+) 7 }
#------------------------------------------------------------------------------
# Switch to workspace 7 using:
# 1. { $mainMod (+) 7 }
#------------------------------------------------------------------------------
bind = $mainMod, 7, workspace, 7
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Cambia al espacio de trabajo 8 usando:
# 1. { $mainMod (+) 8 }
#------------------------------------------------------------------------------
# Switch to workspace 8 using:
# 1. { $mainMod (+) 8 }
#------------------------------------------------------------------------------
bind = $mainMod, 8, workspace, 8
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Cambia al espacio de trabajo 9 usando:
# 1. { $mainMod (+) 9 }
#------------------------------------------------------------------------------
# Switch to workspace 9 using:
# 1. { $mainMod (+) 9 }
#------------------------------------------------------------------------------
bind = $mainMod, 9, workspace, 9
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Cambia al espacio de trabajo 10 usando:
# 1. { $mainMod (+) 0 }
#------------------------------------------------------------------------------
# Switch to workspace 10 using:
# 1. { $mainMod (+) 0 }
#------------------------------------------------------------------------------
bind = $mainMod, 0, workspace, 10
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------

#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el espacio de trabajo 1 al monitor actual y cambia a él usando:
# 1. { $mainMod (+) 1 }
#------------------------------------------------------------------------------
# Move workspace 1 to current monitor and switch to it using:
# 1. { $mainMod (+) 1 }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, 1, moveworkspacetomonitor, 1 current
bind = $mainMod + SHIFT, 1, workspace, 1
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el espacio de trabajo 2 al monitor actual y cambia a él usando:
# 1. { $mainMod (+) 2 }
#------------------------------------------------------------------------------
# Move workspace 2 to current monitor and switch to it using:
# 1. { $mainMod (+) 2 }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, 2, moveworkspacetomonitor, 2 current
bind = $mainMod + SHIFT, 2, workspace, 2
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el espacio de trabajo 3 al monitor actual y cambia a él usando:
# 1. { $mainMod (+) 3 }
#------------------------------------------------------------------------------
# Move workspace 3 to current monitor and switch to it using:
# 1. { $mainMod (+) 3 }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, 3, moveworkspacetomonitor, 3 current
bind = $mainMod + SHIFT, 3, workspace, 3
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el espacio de trabajo 4 al monitor actual y cambia a él usando:
# 1. { $mainMod (+) 4 }
#------------------------------------------------------------------------------
# Move workspace 4 to current monitor and switch to it using:
# 1. { $mainMod (+) 4 }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, 4, moveworkspacetomonitor, 4 current
bind = $mainMod + SHIFT, 4, workspace, 4
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el espacio de trabajo 5 al monitor actual y cambia a él usando:
# 1. { $mainMod (+) 5 }
#------------------------------------------------------------------------------
# Move workspace 5 to current monitor and switch to it using:
# 1. { $mainMod (+) 5 }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, 5, moveworkspacetomonitor, 5 current
bind = $mainMod + SHIFT, 5, workspace, 5
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el espacio de trabajo 6 al monitor actual y cambia a él usando:
# 1. { $mainMod (+) 6 }
#------------------------------------------------------------------------------
# Move workspace 6 to current monitor and switch to it using:
# 1. { $mainMod (+) 6 }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, 6, moveworkspacetomonitor, 6 current
bind = $mainMod + SHIFT, 6, workspace, 6
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el espacio de trabajo 7 al monitor actual y cambia a él usando:
# 1. { $mainMod (+) 7 }
#------------------------------------------------------------------------------
# Move workspace 7 to current monitor and switch to it using:
# 1. { $mainMod (+) 7 }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, 7, moveworkspacetomonitor, 7 current
bind = $mainMod + SHIFT, 7, workspace, 7
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el espacio de trabajo 8 al monitor actual y cambia a él usando:
# 1. { $mainMod (+) 8 }
#------------------------------------------------------------------------------
# Move workspace 8 to current monitor and switch to it using:
# 1. { $mainMod (+) 8 }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, 8, moveworkspacetomonitor, 8 current
bind = $mainMod + SHIFT, 8, workspace, 8
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el espacio de trabajo 9 al monitor actual y cambia a él usando:
# 1. { $mainMod (+) 9 }
#------------------------------------------------------------------------------
# Move workspace 9 to current monitor and switch to it using:
# 1. { $mainMod (+) 9 }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, 9, moveworkspacetomonitor, 9 current
bind = $mainMod + SHIFT, 9, workspace, 9
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve el espacio de trabajo 10 al monitor actual y cambia a él usando:
# 1. { $mainMod (+) 0 }
#------------------------------------------------------------------------------
# Move workspace 10 to current monitor and switch to it using:
# 1. { $mainMod (+) 0 }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, 0, moveworkspacetomonitor, 10 current
bind = $mainMod + SHIFT, 0, workspace, 10
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Abre/cierra el espacio de trabajo especial usando:
# 1. { $mainMod (+) BACKSPACE }
#------------------------------------------------------------------------------
# Opens/closes the special workspace using:
# 1. { $mainMod (+) BACKSPACE }
#------------------------------------------------------------------------------
bind = $mainMod, BACKSPACE, togglespecialworkspace,
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------

#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo 1 sin cambiar a él usando:
# 1. { $mainMod (+) CONTROL (+) 1 }
#------------------------------------------------------------------------------
# Move the focused window to workspace 1 without switching to it using:
# 1. { $mainMod (+) CONTROL (+) 1 }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, 1, movetoworkspacesilent, 1
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo 2 sin cambiar a él usando:
# 1. { $mainMod (+) CONTROL (+) 2 }
#------------------------------------------------------------------------------
# Move the focused window to workspace 2 without switching to it using:
# 1. { $mainMod (+) CONTROL (+) 2 }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, 2, movetoworkspacesilent, 2
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo 3 sin cambiar a él usando:
# 1. { $mainMod (+) CONTROL (+) 3 }
#------------------------------------------------------------------------------
# Move the focused window to workspace 3 without switching to it using:
# 1. { $mainMod (+) CONTROL (+) 3 }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, 3, movetoworkspacesilent, 3
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo 4 sin cambiar a él usando:
# 1. { $mainMod (+) CONTROL (+) 4 }
#------------------------------------------------------------------------------
# Move the focused window to workspace 4 without switching to it using:
# 1. { $mainMod (+) CONTROL (+) 4 }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, 4, movetoworkspacesilent, 4
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo 5 sin cambiar a él usando:
# 1. { $mainMod (+) CONTROL (+) 5 }
#------------------------------------------------------------------------------
# Move the focused window to workspace 5 without switching to it using:
# 1. { $mainMod (+) CONTROL (+) 5 }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, 5, movetoworkspacesilent, 5
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo 6 sin cambiar a él usando:
# 1. { $mainMod (+) CONTROL (+) 6 }
#------------------------------------------------------------------------------
# Move the focused window to workspace 6 without switching to it using:
# 1. { $mainMod (+) CONTROL (+) 6 }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, 6, movetoworkspacesilent, 6
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo 7 sin cambiar a él usando:
# 1. { $mainMod (+) CONTROL (+) 7 }
#------------------------------------------------------------------------------
# Move the focused window to workspace 7 without switching to it using:
# 1. { $mainMod (+) CONTROL (+) 7 }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, 7, movetoworkspacesilent, 7
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo 8 sin cambiar a él usando:
# 1. { $mainMod (+) CONTROL (+) 8 }
#------------------------------------------------------------------------------
# Move the focused window to workspace 8 without switching to it using:
# 1. { $mainMod (+) CONTROL (+) 8 }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, 8, movetoworkspacesilent, 8
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo 9 sin cambiar a él usando:
# 1. { $mainMod (+) CONTROL (+) 9 }
#------------------------------------------------------------------------------
# Move the focused window to workspace 9 without switching to it using:
# 1. { $mainMod (+) CONTROL (+) 9 }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, 9, movetoworkspacesilent, 9
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo 10 sin cambiar a él usando:
# 1. { $mainMod (+) CONTROL (+) 0 }
#------------------------------------------------------------------------------
# Move the focused window to workspace 10 without switching to it using:
# 1. { $mainMod (+) CONTROL (+) 0 }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, 0, movetoworkspacesilent, 10
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada al espacio de trabajo especial sin cambiar a él
# usando:
# 1. { $mainMod (+) CONTROL (+) BACKSPACE }
#------------------------------------------------------------------------------
# Move the focused window to the special workspace without switching to it
# using:
# 1. { $mainMod (+) CONTROL (+) BACKSPACE }
#------------------------------------------------------------------------------
bind = $mainMod + CONTROL, BACKSPACE, movetoworkspacesilent, special
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------

#------------------------------------------------------------------------------
# Desplaza por los espacios de trabajo existentes usando:
# 1. { $mainMod (+) SCROLL }
#------------------------------------------------------------------------------
# Scroll through existing workspaces using:
# 1. { $mainMod (+) SCROLL }
#------------------------------------------------------------------------------
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
#------------------------------------------------------------------------------

#------------------------------------------------------------------------------
# Intercambia los espacios de trabajo activos entre monitores usando:
# 1. { $mainMod (+) SPACE }
#------------------------------------------------------------------------------
# Swap the active workspaces between monitors using:
# 1. { $mainMod (+) SPACE }
#------------------------------------------------------------------------------
bind = $mainMod, SPACE, swapactiveworkspaces, DP-2 DP-1
#------------------------------------------------------------------------------

#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada a la izquierda usando:
# 1. { $mainMod (+) SHIFT (+) H }
#------------------------------------------------------------------------------
# Move the focused window left using:
# 1. { $mainMod (+) SHIFT (+) H }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, H, movewindow, l
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada hacia abajo usando:
# 1. { $mainMod (+) SHIFT (+) J }
#------------------------------------------------------------------------------
# Move the focused window down using:
# 1. { $mainMod (+) SHIFT (+) J }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, J, movewindow, d
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada hacia arriba usando:
# 1. { $mainMod (+) SHIFT (+) K }
#------------------------------------------------------------------------------
# Move the focused window up using:
# 1. { $mainMod (+) SHIFT (+) K }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, K, movewindow, u
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
# Mueve la ventana enfocada a la derecha usando:
# 1. { $mainMod (+) SHIFT (+) L }
#------------------------------------------------------------------------------
# Move the focused window right using:
# 1. { $mainMod (+) SHIFT (+) L }
#------------------------------------------------------------------------------
bind = $mainMod + SHIFT, L, movewindow, r
#------------------------------------------------------------------------------
#------------------------------------------------------------------------------
###############################################################################
